# Examples

示例项目不内置在 Impeccable Fork 中。

React Bits 示例项目应作为独立的 React + Vite 项目维护。这样可以让这个 Fork 保持接近上游，同时让示例项目自由选择 React、Vite、React Bits 和其他 UI 依赖。

对示例项目运行 Impeccable 时，请从该项目根目录调用：

```bash
$impeccable init
$impeccable audit
$impeccable polish
```
